const linkStyle = {
  color: '#FFF',
};

const layoutStyle = {
  border: '1px solid #DDD',
};

const titleStyle = {
  cursor: 'pointer',
  fontWeight: 'bold',
};

const floatingBtnStyle = {
  margin: 0,
  top: 'auto',
  right: 20,
  bottom: 20,
  left: 'auto',
  position: 'absolute',
  zIndex: 2,
};

const floatingBtnCloseStyle = {
  margin: 0,
  top: 'auto',
  right: 20,
  bottom: 50,
  left: 'auto',
  position: 'absolute',
  zIndex: 2,
};

const avatarStyle = {
  cursor: 'pointer',
  marginTop: 3,
};

export {
  linkStyle,
  layoutStyle,
  titleStyle,
  floatingBtnStyle,
  avatarStyle,
  floatingBtnCloseStyle,
};
